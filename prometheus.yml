global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alert-rules.yml"

scrape_configs:
  - job_name: 'log-analyzer'
    static_configs:
      - targets: ['host.docker.internal:8000']
    metrics_path: '/metrics'
