groups:
  - name: log-analyzer-alerts
    rules:
      - alert: HighErrorRate
        expr: log_levels_total{level="ERROR"} > 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "High error rate detected"
          description: "Error rate is above 0 in log analyzer (current value: {{ $value }})"
